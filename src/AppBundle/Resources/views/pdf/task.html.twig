<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/8.0.1/normalize.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/paper-css/0.4.1/paper.min.css">
    <style>
    @page {
      size: A4
    }
    header {
      display: flex;
      justify-content: space-between;
      margin-bottom: 10mm;
    }
    h3 {
      text-align: center;
    }
    .title {
      font-variant: small-caps;
      border: 1px solid #333;
      padding: 5mm;
      margin-bottom: 10mm;
    }
    </style>
    {# <link rel="stylesheet" href="https://unpkg.com/papercss@1.6.1/dist/paper.min.css"> #}
    {# <title>Document</title> #}
    {# <meta charset="UTF-8" />
    <link rel="stylesheet" href="https://unpkg.com/purecss@1.0.1/build/pure-min.css" integrity="sha384-oAOxQR6DkCoMliIh8yFnu25d7Eq/PHS21PClpwjOTeU2jRSq11vu66rf90/cZr47" crossorigin="anonymous">
    https://unpkg.com/papercss@1.6.1/dist/paper.min.css #}
  </head>
  <body class="A4">

    <section class="sheet padding-10mm">
      <header>
        <div>
          <img src="{{ company_logo }}" width="160" height="160">
        </div>
        <div>
          <ul style="text-align: right">
            <li>{{ coopcycle_setting('brand_name') }}</li>
            <li>{{ 'now'|localizeddate('short', 'short') }}</li>
          </ul>
        </div>
      </header>

      <div class="title">
        <h3>{{ 'proof_of_delivery.title'|trans }}</h3>
        <p style="text-align: center;">{{ task.address.streetAddress }}</p>
      </div>

      <div style="display: flex;">
      {% for image in task.images %}
        <div style="border: 1px solid #000; padding: 10mm;">
          <div>
            <img src="{{ image|image_to_base64 }}" style="width: 50mm;">
          </div>
          <span>{{ image.createdAt|localizeddate('short', 'short') }}</span>
        </div>
      {% endfor %}
      </div>
    </section>

  </body>
</html>
